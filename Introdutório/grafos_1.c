#include <stdio.h>
#include <stdlib.h>

int main(){

    // Grafos 
    // é definido como um conjunto de vértices e um conjunto de arestas 
    // que conectam qualquer par de vértices

    // G = (V,A);
    // V - é o conjunto de vértices (não vazio)
    // A - é o conjunto de arestas

    G(V,A)
    V = {1 , 2 , 3, 4}
    A = {{1,2},{1,4},{2,3},{3,4}}

    // Vértice
    // É cada uma das entidades representadas em um grafo.
    // Pode ser pessoas, casas, etc.
    // Se há 2 ( dois ) vértices conectados então é um "ADJACENTES"


    // Aresta
    // também chamada de arco
    // é associada a 2 vértices (v1,v2)
    // faz a ligação entre eles ou seja diz qual a relação entre eles
    return 0;
}

